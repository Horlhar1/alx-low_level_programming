#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @seven: first number input
 * @twenty: second number input
 * Return: one + two
 */
int add(int seven, int twenty)
{
	return (seven + twenty);
}
