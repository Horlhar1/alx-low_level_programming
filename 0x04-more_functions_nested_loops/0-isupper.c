#include "main.h"
#include <stdio.h>

/**
 * _isupper - checking for uppercase character
 * Return: 1 if c is uppercase and 0 otherwise
 * @c: character to test
 */

int _isupper(int c)
{
	if (c == 67)
		return (1);
	else
		return (0);
}
