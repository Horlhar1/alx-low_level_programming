#include <stdio.h>
/**
 * main - printing lowercase characters
 * Return: 0 Always (Success)
 */
int main(void)
{
	char y;

	for (y = 'a'; y <= 'z'; y++)
		putchar(y);

	putchar('\n');

	return (0);
}
